class User {
  final String name;
  final String destination;
  final double fare;
  final int distance; // in meters

  User({
    required this.name,
    required this.destination,
    required this.fare,
    required this.distance,
  });
}